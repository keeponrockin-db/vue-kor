{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "mocha --timeout 5000 -r ts-node/register src/**/*.spec.ts"
  },
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@types/express": "^4.17.8",
    "@types/mocha": "^8.0.3",
    "assert": "^2.0.0",
    "axios": "^0.20.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.1",
    "mongodb": "^3.6.1"
  },
  "devDependencies": {
    "@types/mongodb": "^3.5.27",
    "firebase-functions-test": "^0.2.2",
    "mocha": "^8.1.3",
    "ts-node": "^9.0.0",
    "tslint": "^5.12.0",
    "typescript": "^4.0.2"
  },
  "private": true
}
